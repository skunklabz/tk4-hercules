name: tkx-hercules-tk4

services:
  tkx-hercules:
    extends:
      file: ./versions/${MVS_VERSION:-tk4}/docker-compose.yml
      service: tkx-hercules

volumes:
  # TK4- volumes
  tk4-conf:
  tk4-local_conf:
  tk4-local_scripts:
  tk4-prt:
  tk4-dasd:
  tk4-pch:
  tk4-jcl:
  tk4-log: